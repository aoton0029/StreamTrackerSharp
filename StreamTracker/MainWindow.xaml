<Window x:Class="StreamTracker.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StreamTracker"
        xmlns:views="clr-namespace:StreamTracker.UserControls"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <DockPanel>

        <!-- トップパネル（DockPanel上部）-->
        <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Background="#222">
            <Button Content="ログイン" Command="{Binding LoginCommand}" Margin="0,0,10,0"/>
        </StackPanel>

        <!-- メインパネル：配信カードをグリッド状に -->
        <ScrollViewer>
            <ItemsControl ItemsSource="{Binding LiveStreams}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <views:StreamCard />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

    </DockPanel>

</Window>
